import { HomeLayout, NotFoundMain } from "@/components";
import React from "react";

const NotFound = () => {
  return (
    <HomeLayout>
      <NotFoundMain />
    </HomeLayout>
  );
};

export default NotFound;
